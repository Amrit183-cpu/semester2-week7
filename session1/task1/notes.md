In the C program, it raises an exception on the last line 
In the python program, it deciphers where the error was saying x[i]

Python would be easier to debug because it mentions more information e.g. what and where the error is 